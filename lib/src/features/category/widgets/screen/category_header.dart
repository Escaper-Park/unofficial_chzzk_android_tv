part of '../../category_screen.dart';

class _CategoryHeader extends StatelessWidget {
  const _CategoryHeader();

  @override
  Widget build(BuildContext context) {
    return HeaderText(text: '카테고리');
  }
}
