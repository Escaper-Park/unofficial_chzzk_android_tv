import 'package:flutter/material.dart';

class VodChatStream extends StatelessWidget {
  const VodChatStream({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
