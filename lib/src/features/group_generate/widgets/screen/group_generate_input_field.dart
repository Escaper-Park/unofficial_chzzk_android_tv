part of '../../group_generate_screen.dart';

class _GroupGenerateInputField extends StatelessWidget {
  const _GroupGenerateInputField();

  @override
  Widget build(BuildContext context) {
    return VirtualKeyboardInputField(
      routeName: AppRoute.groupGenerate.routeName,
    );
  }
}
