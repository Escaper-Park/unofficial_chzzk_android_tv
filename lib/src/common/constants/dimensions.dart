import 'dart:ui';

class Dimensions {
  // Splash
  static const double splashLogoSize = 100.0;

  // Dashboard
  static const double collapsedSidebarWidth = 60.0;
  static const double expandedSidebarWidth = 200.0;

  // Auth
  static const double naverLoginWebViewWidth = 400.0;

  // Home
  static const double homeChzzkTextLogoWidth = 150.0;
  static const Size followingCategoryContainerSize = Size(120.0, 150.0);
  static const Size exceptionFallbackWidgetSize = Size(300.0, 50.0);

  // Following
  static const double followingListWidth = 220.0;
  static const Size followingButtonSize = Size(100.0, 40.0);

  // Video
  static const Size videoThumbnailSize = Size(256.0, 144.0);
  static const double videoContainerWidth = 256.0;
  static const double videoContainerHeight = 220.0;

  // Category List
  static const Size categoryHeaderPosterImageSize = Size(90.0, 120.0);
  static const double categoryInfoCardHeight = 100.0;

  // Category Detail
  static const Size categoryInfoSize = Size(200.0, 120.0);

  // Vod streaming
  static const double vodStreamInfoContainerHeight = 120.0;
}
