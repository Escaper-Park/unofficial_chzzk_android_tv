import 'dart:ui';

class Dimensions {
  // Dashboard
  static const double logoIconWidth = 45.0;
  static const double sidebarWidth = 200.0;

  // Auth
  static const double loginFormContainerWidth = 350.0;

  // user
  static const double profileImageContainerHeight = 50.0;

  // home
  static const double homeRefreshButtonWidth = 125.0;
  static const Size homeFavoriteCategorySize = Size(120.0, 150.0);

  // live
  static const Size liveThumbnailSize = Size(256.0, 144.0);
  static const double homeBaseContainerHeight = 220.0;

  // Live Streaming
  static const double liveStreamingMainControlOverlayHeight = 110.0;
  static const double chatSettingsControlContainerWidth = 250.0;

  // following
  static const double followingListWidth = 220.0;

  // search
  static const double searchFormWidth = 500.0;

  // category live
  static const Size categoryLiveHeaderPosterSize = Size(90.0, 120.0);
  static const Size categoryLiveInfoSize = Size(200.0, 120.0);
  static const double favoriteButtonWidth = 100.0;
  static const double liveStreamingInfoContainerHeight = 100.0;

  // VOD Streaming
  static const double vodStreamingControlOverlayHeight = 150.0;
  static const double vodStreamingInfoContainerHeight = 100.0;
}
